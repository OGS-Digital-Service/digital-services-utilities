<section class="nysds-pillars" id="nysds-pillar-id">
{% if type == 'hollow'%}

    {% for card in params.pillarCards %}
    {% endfor %}

{% else %}
    
    <div class="nysds-pillars-headline"> {{params.headline}} </div>
    <div class="nysds-pillars-container">
    {% for card in params.pillarCards %}
        <div  class="nysds-pillars-cards">
            <div class="nysds-pillars-icon" aria-hidden="true">
                {{card.icon | safe}}
            </div>
            <h2 class="nysds-pillars-title">
                {{card.title}}
            </h2>
            <div class="nysds-pillars-list-container">
                <ul class="nysds-pillars-card-list">
                    {%for check in card.checklist%}
                    <li>
                        {# <span class="nysds-pillars-check" aria-hidden="true"></span> #}
                        <span>{{check.text}}</span>
                    </li>
                    {% endfor %}
                </ul>
            </div>
            <a class="nysds-pillar-link" href="{{card.link}}" aria-label="{{card.ariaLabel}}">{{card.linkText}} <span class="nysds-pillars-arrow" aria-hidden="true"><span></a>
        </div>
    {% endfor %}
    </div>
   
{% endif %}
</section>
<script>
    {% include './_pillars.js'  %}
</script>