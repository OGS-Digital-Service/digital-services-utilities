<section class="flex flex-col -mx-8 md:-mx-20 my-14">
<h2 class="nysds-text-36 font-extrabold text-center mb-4 w-full">{{params.frametitle}}</h2>
<div class="flex flex-col md:flex-row">

{% for card in params.cards %}
<article class="w-1/3">
<img class="" src="" />
<a class="" href="{{card.link}}">
<h3 class="">{{card.title}}</h3>
</a>
<p class="">{{card.content}}</p>
</article>
{% endfor %}

</div>
</section>