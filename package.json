{
  "scripts": {
    "start": "eleventy --serve & npx tailwindcss -i tailwind-setup.css -o css/tailwind.css --minify --watch",
    "test":"pa11y-ci"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "tailwindcss": "^3.0.23",
    "@11ty/eleventy": "^1.0.0"
  }
}
