{
  "scripts": {
    "start": "npx eleventy --serve & npx tailwindcss -i tailwind-setup.css -o css/tailwind.css --minify --watch",
    "eleventyOnly": "npx @11ty/eleventy --serve",
    "test": "pa11y-ci"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.0",
    "pa11y-ci-reporter-html": "^5.0.2",
    "tailwindcss": "^3.0.23"
  }
}
